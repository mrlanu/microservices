spring:
  cloud:
    config:
      name: auth-service
      discovery:
        service-id: config
        enabled: true
      fail-fast: true
      password: 1234
      username: user

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8001/eureka/
