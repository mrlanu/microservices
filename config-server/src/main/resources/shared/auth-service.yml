spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      username: oauth-user
      password: password
      database: oauth-db
  application:
    name: auth-service

server:
  servlet:
    context-path: /uaa
  port: 8002
